# Optional access for the GestureForge uinput action provider.
# Install system-wide only when virtual input actions are intentionally enabled.
KERNEL=="uinput", GROUP="input", MODE="0660", TAG+="uaccess"
